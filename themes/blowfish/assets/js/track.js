import params from "@params";

assets


window.minimalAnalytics = {
  trackingId: params.trackingId,
  window.minimalAnalytics = {
    trackingId: params.trackingId,
    autoTrack: true,
    defineGlobal: true,
  };
  defineGlobal: true,
};